<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <item name="android:textViewStyle">@style/HouseTextViewStyle</item>
        <item name="android:buttonStyle">@style/HouseButtonStyle</item>
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="SelectTheme" parent="android:Theme.Dialog"></style>

    <style name="SelectTheme.TransparentTest">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="HouseTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">NotoSansKR-Black.otf</item>
    </style>

    <style name="HouseButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:fontFamily">NotoSansKR-Light.otf</item>
    </style>

</resources>